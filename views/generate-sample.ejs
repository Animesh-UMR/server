<!DOCTYPE html>
<html>

<head>
    <title>Search Bar</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
    <!-- Custom CSS -->
    <style>
        body {
            display: flex;
            flex-direction: column;
            height: 100vh;
            justify-content: center;
            align-items: center;
        }

        .search-info {
            text-align: center;
            margin-bottom: 50px;
            font-size: 30px;
            font-weight: bold;
            color: #333;
        }

        .search-box {
            display: flex;
            flex-direction: row;
            align-items: center;
            background-color: #b2d8b2;
            /* pastel green */
            padding-right: 2px;
            padding-left: 20px;
            border-radius: 50px;
            width: 600px;
            height: 60px;
            box-shadow: 0px 1px 5px 0px rgba(0, 0, 0, 0.2);
        }

        .search-box input {
            border: none;
            background-color: transparent;
            width: 100%;
            margin-right: 10px;
            font-size: 20px;
            font-weight: bold;
            line-height: 20px;
            color: #333;
            outline: none;
        }

        .search-box input:focus {
            box-shadow: none;
        }

        .search-box button {
            background-color: transparent;
            border: none;
            cursor: pointer;
            outline: none;
        }

        .search-box button:focus {
            box-shadow: none;
        }

        .search-icon {
            background-color: #007bff;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #fff;
        }

        .search-icon i {
            font-size: 18px;
        }
    </style>
</head>

<body>
    <div class="search-info">Generate Sample For Any Market</div>
    <form action="/getsample" method="post">
        <div class="search-box">
            <input type="text" name="keyword" placeholder="Search...">
            <button type="submit">
                <div class="search-icon">
                    <i class="fas fa-search"></i>
                </div>
            </button>
        </div>
    </form>

    <!-- Bootstrap JS and Font Awesome JS -->
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
    <script src="https://kit.fontawesome.com/ccf00e2591.js" crossorigin="anonymous"></script>
</body>

</html>